// import { createSlice } from "@reduxjs/toolkit";

// const taskSlice = createSlice({
//     name: 'task',
//     initialState: [],
//     reducers: {
//         handleAddItems: (state, actions) => {
//             const existingItem = state.some((i) => i.id === actions.payload);

//             if (!existingItem) {
//                 state.push()
//             },
// })